# 0136 Single Number

Pattern: Hashing

## Approach
Use a HashSet:

- Add number if not present
- Remove if already present

At the end, only the single number remains.

## Time Complexity
O(n)

## Space Complexity
O(n)