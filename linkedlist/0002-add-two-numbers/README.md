# 0002 Add Two Numbers

Pattern: Linked List + Simulation

## Approach
- Traverse both lists
- Add digits + carry
- Create new node for each digit
- Maintain carry

## Time Complexity
O(max(m, n))

## Space Complexity
O(max(m, n))